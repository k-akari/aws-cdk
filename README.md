# How to Use
```bash
docker-compose up -d
docker-compose exec node ash
```

# Create CDK Project(only first time)
```bash
docker-compose exec node ash
mkdir cdk
cd cdk
cdk init app --language=typescript
```
